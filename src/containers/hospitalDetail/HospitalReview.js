import React from 'react';
import {View, Text} from 'react-native';
import {TopContainerView} from '../../components/common/View';

const HospitalReview = props => {
  return (
    <TopContainerView marginTop={10}>
      <Text>병원 리뷰 페이지</Text>
    </TopContainerView>
  );
};

export default HospitalReview;
