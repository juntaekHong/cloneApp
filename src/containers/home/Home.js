import React from 'react';
import {Text} from 'react-native';
import {CenterView} from '../../components/common/Extra';

const Home = props => {
  return (
    <CenterView>
      <Text>Main Page</Text>
    </CenterView>
  );
};

export default Home;
